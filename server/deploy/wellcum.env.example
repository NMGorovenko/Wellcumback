# Generate a private key before starting. Never commit the resulting env file.
WELLCUM_ACCESS_KEY=REPLACE_WITH_64_RANDOM_HEX_CHARACTERS
HOST=127.0.0.1
PORT=8787
WELLCUM_DATABASE=/var/lib/wellcum/rooms.sqlite

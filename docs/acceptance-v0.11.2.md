# Приёмка 0.11.2 — восстановление туннеля

## Автоматические проверки

- TypeScript, lint и все 394 теста прошли, без пропущенных. Логи: `outputs/tests-v0112-final.log` и `outputs/tests-v0112-recovery.log`.
- Тест аварии использует настоящий loopback relay/SQLite, отдельные WebSocket-соединения создателя и гостя и подставной провайдер туннеля. После аварии, неудачной попытки и следующего успешного запуска сохранены порт relay, ключ, код, личные токены, роли, состояние и идентификатор попытки. Мир остановлен, очередь старого ввода очищена, гость отмечен отсутствующим.
- Параллельные retry создают один туннель. Запоздалые callbacks игнорируются. Остановка во время retry закрывает даже поздний результат провайдера. Последний stop отменяет host, ожидающий предыдущую остановку. Смена режима не удаляет восстановимую комнату.
- Для города, экрана, казармы и переезда новая строка начинает poll/rejoin с прежним токеном, без join и старых нажатий. Сохраняются роль, состояние, попытка и новое личное подключение. Возврат не снимает паузу. Другая комната, ключ или небезопасный URL отклоняются до отправки токена.
- Поздний ответ старого endpoint не меняет восстановленный мир и не создаёт второй цикл polling. Закрытие комнаты во время восстановления не привязывает его результат к новой сессии.
- Независимый обзор кода и повторный прогон девяти целевых проверок не нашли оставшихся проблем в этой итерации.

## Сборки и приложение

- Веб-сборка, Mac arm64/x64 ZIP/DMG и Windows portable/setup EXE завершились успешно. Логи: `outputs/build-v0112.log`, `outputs/desktop-mac-v0112.log`, `outputs/desktop-win-v0112.log`.
- Реальный Electron на Mac arm64 прошёл все фазы native smoke: сохранения между запусками, клавиатура, программные Xbox/DualSense, комиксы переезда, создание комнаты через интерфейс выбора сети, отдельный Node-гость, четыре сцены и ACK. Лог `outputs/desktop-smoke-v0112.log`.
- Самостоятельный сервер прошёл миграцию старой базы, перезапуск, сохранённую паузу и независимый LAN transport: `outputs/server-v0112.log`.
- Локальное окно «Онлайн» открылось в IAB. Первоначальная изолированная браузерная проверка новой строки остановилась на сетевой странице ошибки. Позже этот пробел закрыт отдельным Electron smoke ниже. Пользовательское сохранённое подключение не изменялось.

## Дополнительная приёмка интерфейса, 13 сентября 2026

`node scripts/smoke-recovery.mjs` прошёл обе фазы на подготовленном приложении 0.11.2, macOS arm64. Проверены настоящие main, preload, renderer, IPC и WebSocket/SQLite relay. Заменён только провайдер туннеля внутри тестового процесса; пользовательские профили и сервер на `localhost:3001` не используются.

- Создатель через интерфейс создаёт комнату, едет нативной клавишей W и передаёт ведущего отдельному гостю. Авария провайдера скрывает старое приглашение и открывает восстановление. Первая попытка намеренно неудачна, следующая возвращает новый адрес с прежним кодом, ключом и личным токеном. На сервере сохраняются реально изменённая позиция, время, попытка, роли и пауза. Явное закрытие комнаты останавливает сервер.
- Гость через интерфейс вставляет новую строку. Наблюдение реальных запросов приложения подтверждает успешный `poll/rejoin` по новому адресу с прежним токеном и местом, без нового `join`. Получен прежний снимок: попытка 17, время 143, координата X=42, пауза. На экране «Продолжит ведущий» недоступно гостю; возврат не запускает время. Затем проверен явный выход из комнаты.
- Передача роли сама устанавливает паузу до аварии в фазе создателя. Этот smoke доказывает её сохранение; переход из движения в паузу при аварии отдельно проверяет `desktop-network.test.mjs`.
- Исправлены ошибки самого теста: нативный ввод ждёт фокус окна, новое приглашение заменяет старый текст целиком, успешный возврат определяется фактическим ответом нового endpoint. Независимый обзор проверил эти условия.

Лог: `outputs/recovery-ui-v0112.log`. Снимок паузы гостя: `outputs/recovery-guest.png`. Запись успешного запуска: `outputs/recovery-ui.json`, 2026-09-13 08:39:50 UTC. SHA-256 проверенных runtime-файлов:

| Файл | SHA-256 |
| --- | --- |
| main.cjs | fe7febce98c571dca207b9443db2d176c70007481402ae6d27d3cc1f04962ae3 |
| preload.cjs | 426857846b7b5ec53851c5d9500057c10c73ae995f706801e32a7a8101ffe307 |
| network.cjs | b11734cd2d9db9c0d5108df2c5cd7867eed1cd93f663252830e1f9109137cd85 |
| security.cjs | c668e03531f42fee668391be8b4ff1f87225739e8b0529621b858cd698b7ce0e |
| renderer/index.html | f6fcafb2a8e931a656ccbf1704753a985395849acdb0180ae8675765123012f6 |

Это дополнительная проверка уже выпущенных runtime-файлов, без изменения игры, версии или тега 0.11.2. Исходники smoke и отчёт сохраняются следующим коммитом в основной ветке; исходный архив релиза остаётся привязан к его тегу.

## Пределы проверки

Подставной провайдер проверяет восстановление жизненного цикла, а не публичный TLS-туннель. Текущая сеть по-прежнему блокирует соединение с Cloudflare; повторные одинаковые внешние пробы не выполнялись. Межсетевой Windows/Mac, физические геймпады и Intel Mac не проверены. Установка VPN, VPS, аккаунты и расширение аудитории сайта не выполнялись. Закрытие процесса сервера или выключение компьютера создателя завершает встроенный сеанс; перенос процесса сервера на другого участника не входит в передачу игровой роли.
